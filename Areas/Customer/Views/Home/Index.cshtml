@using Online_Shop.Models;
@using X.PagedList.Mvc.Core;
@model X.PagedList.IPagedList<Products>
@{
    ViewData["Title"] = "Home Page";
}
</br></br>
<div class="row">
    @foreach (var product in Model)
    {


        <div class="col-md-3">

            <div class="card mb-4">
                <div class="card-header">
                    <h4 class="my-0 font-weight-normal">
                        <label style="font-size:15px; color:steelblue;">@product.Name</label>
                    </h4>




                </div>
                <img src="~/@product.Image" alt="Card Image" class="card-image-top" style="height:150px;" />
              
                <div class="card-header">

                    <div class="d-flex justify-content-between align-items-center">
                        <div class="btn-group">
                            <label style="font-size:15px; color:#a51313"><b>Price: @product.Price &nbsp</b></label>
                            <a asp-action="Details" asp-controller="Home" asp-route-id="@product.Id" class="btn btn-primary ">Details</a>
                        </div>
                    </div>
                </div>

            </div>
        </div>


    }
</div>
<div >
    @Html.PagedListPager(Model,c=>Href("~/Customer/Home/Index?page="+c) )
</div>




